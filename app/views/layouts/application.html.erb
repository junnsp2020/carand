<!DOCTYPE html>
<html>
  <head>
    <title>Carand</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="navbar navbar-fixed-top" style="background-color:#f79428;">
      <nav class="nav navbar-nav navbar-left">
          <span class="glyphicon glyphicon-star" aria-hidden="true" style="font-size: 11px"></span>
          <span class="glyphicon glyphicon-star" aria-hidden="true" style="font-size: 11px"></span>
          <%= link_to root_path do %>
              <span class="title">カランド</span>
          <% end %>
          <span class="glyphicon glyphicon-star" aria-hidden="true" style="font-size: 8px"></span>
          <span class="glyphicon glyphicon-star" aria-hidden="true" style="font-size: 8px"></span>
          <span class="center-block justify-content-center">
            <%= form_with url: product_search_path, method: :get, data: {remote: false} do |f| %>
              <%= f.text_field :name , placeholder: "何をお探しですか？"%>
            <%= f.submit :🔍 %>
          </span>
          <% end %>

          <%# if Product.where(user_id: current_user.id).count != 0 && BarterRequest.where(notice: true).count != 0 %>
            <%#= "hoge" %>
          <%# end %>

          <div class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              カテゴリーからもお探しできます
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to "全て", products_path %>
              </li>
              <li>
                <% Category.all.each do |category| %>
              </li>
              <li>
                <%= link_to category.name, products_path(category_id: category.id) %>
              </li>
                <% end %>
            </ul>
          </div>
        <!-- </span> -->
      </nav>
        <ul class="nav navbar-nav navbar-right">
          <% if user_signed_in? %>
            <% if Product.where(user_id: current_user.id, notice: false).count != 0 %>
              <%= link_to "交換リクエストがきています　内容を除いてみましょう", requested_barter_requests_path %>
            <% end %>
            <li class="wishlists"><%= link_to "ほしいものリスト", wish_wishlists_path %></li>
            <li>
              <span class="trading-list">
                <%= link_to "買った！", bought_tradings_path %>
                  <% if Trading.where("(payment_status = ?) OR (payment_status = ?) ", 0, 2).where(buyer_id: current_user.id).count == 0 %>
                    <span class="badge1">
                      <span class="badge", style="color: white;">
                      <%= Trading.where("(payment_status = ?) OR (payment_status = ?) ", 0, 2).where(buyer_id: current_user.id).count %>
                      </span>
                  </span>
                    <% else %>
                    <span class="badge2">
                      <span class="badge", style="color: white;">
                        <%= Trading.where("(payment_status = ?) OR (payment_status = ?) ", 0, 2).where(buyer_id: current_user.id).count %>
                      </span>
                    </span>
                  <% end %>
            </li>
            <li>
              <span class="trading-list">
                <%= link_to "売った！", sold_tradings_path %>
                  <% if Trading.where("(shipment_status = ?) OR (shipment_status = ?) ", 0, 2).where(seller_id: current_user.id).count == 0 %>
                    <span class="badge3">
                      <span class="badge", style="color: white;">
                      <%= Trading.where("(shipment_status = ?) OR (shipment_status = ?) ", 0, 2,).where(seller_id: current_user.id).count %>
                      </span>
                    </span>
                  <% else %>
                    <span class="badge4">
                      <span class="badge", style="color: white;">
                      <%= Trading.where("(shipment_status = ?) OR (shipment_status = ?) ", 0, 2).where(seller_id: current_user.id).count %>
                      </span>
                    </span>
                  <% end %>
            </li>
            <li class="mypage"><%= link_to "マイページ", user_path(current_user) %></li>
          <% else %>
            <span class="hoge" style="color: white;"><%= link_to "sign_out", destroy_user_session_path, method: :delete %></span>
            <span><%= link_to ' login', new_user_session_path %></span>
            <span><%= link_to "アドミン sign_out", destroy_user_session_path, method: :delete %></span>
            <span><%= link_to 'アドミン login', new_user_session_path %></span>
          <% end %>
        </ul>
    </header>
    <div class="camera">
      <%= link_to "", new_product_path, class: "btn btn-default btn-circle btn-lg glyphicon glyphicon-camera" %>
    </div>
    <%= yield %>
  </body>
</html>